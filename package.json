{
  "name": "react-polybase-web3-app",
  "version": "1.0.0",
  "description": "A React app that uses the Polybase Web3 storage to store the user name and a hash id, and uses the Dynamic SDK for authentication.",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "web3": "^1.3.4",
    "polybase": "^1.0.0",
    "dynamic-sdk": "^1.0.0"
  },
  "devDependencies": {
    "react-scripts": "4.0.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}